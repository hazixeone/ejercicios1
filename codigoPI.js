let contador
let alternada = 1;
let limiite = prompt ("ingresa el numero de terminos")
let PI =0;

for (contador=1; contador < limiite; contador += 2){
    PI += añternada=(1/contador)
    alternada*=(-1);
}
PI
console.log(PI)